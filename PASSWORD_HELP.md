# 🔐 Пароль от сервера Cloud.ru

## Что это за пароль?

Это пароль для доступа к виртуальной машине через SSH. Он нужен для команды:
```bash
ssh root@176.109.111.108
```

## Где найти пароль?

### Вариант 1: В панели Cloud.ru

1. Войдите в панель управления Cloud.ru
2. Перейдите в раздел **Виртуальные машины** (Compute → Instances)
3. Найдите вашу VM с IP `176.109.111.108`
4. Откройте детали VM
5. В разделе **Доступ** или **Пароли** должен быть указан пароль

### Вариант 2: Сброс пароля

Если пароль не был установлен или забыт:

1. В панели Cloud.ru откройте вашу VM
2. Найдите опцию **"Сбросить пароль"** или **"Reset Password"**
3. Cloud.ru сгенерирует новый пароль
4. **ВАЖНО**: Сохраните пароль сразу, он показывается только один раз!

### Вариант 3: Использование SSH ключей

Если вы настроили SSH ключи при создании VM:

1. Используйте SSH ключ вместо пароля:
```bash
ssh -i ~/.ssh/ваш_ключ root@176.109.111.108
```

2. Или добавьте ключ в ssh-agent:
```bash
ssh-add ~/.ssh/ваш_ключ
ssh root@176.109.111.108
```

## Альтернатива: Установка пароля на сервере

Если у вас есть доступ к консоли VM в панели Cloud.ru:

1. Откройте консоль VM в браузере (через панель Cloud.ru)
2. Выполните:
```bash
sudo passwd root
```
3. Введите новый пароль дважды
4. Теперь можно использовать этот пароль для SSH

## Проверка подключения

После получения пароля проверьте подключение:

```bash
ssh root@176.109.111.108
```

Введите пароль, когда попросит.

## Если пароль не работает

1. **Проверьте пользователя**: Возможно, нужно использовать другого пользователя:
   - `ubuntu@176.109.111.108` (для Ubuntu)
   - `debian@176.109.111.108` (для Debian)
   - `root@176.109.111.108` (для root)

2. **Проверьте SSH ключи**: Убедитесь, что не настроены только SSH ключи

3. **Сбросьте пароль** через панель Cloud.ru

## Безопасность

⚠️ **ВАЖНО**: 
- Не делитесь паролем
- После первого подключения рекомендуется настроить SSH ключи
- Используйте сложный пароль

## Настройка SSH ключей (рекомендуется)

После первого подключения настройте SSH ключи для удобства:

```bash
# На вашем Mac
ssh-keygen -t ed25519 -C "your_email@example.com"
ssh-copy-id root@176.109.111.108

# Теперь можно подключаться без пароля
ssh root@176.109.111.108
```

