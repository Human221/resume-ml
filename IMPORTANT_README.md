# ⚠️ ВАЖНО: Где выполнять команды

## 🖥️ На Mac (ваш компьютер)

**НЕ выполняйте команды для Linux!**

На Mac можно только:
- ✅ Подготовить код
- ✅ Проверить датасет (без GPU)
- ✅ Загрузить код в Git

**НЕ работает на Mac:**
- ❌ `apt` (это для Linux)
- ❌ `nvidia-smi` (нет NVIDIA GPU)
- ❌ Обучение модели (нужен GPU)

## 🚀 На сервере Cloud.ru (176.109.111.108)

**ВСЕ команды обучения выполняются ТОЛЬКО на сервере!**

---

## 📋 Правильная последовательность действий

### Шаг 1: На Mac - подготовка (уже сделано ✅)

Код уже готов в репозитории. Можно проверить датасет:

```bash
# На Mac
cd ~/resume-ml
pip install datasets requests
python download_hf_dataset.py --dataset evilfreelancer/headhunter --action list
```

### Шаг 2: Подключение к серверу Cloud.ru

**Откройте новый терминал и подключитесь к серверу:**

```bash
ssh root@176.109.111.108
```

Введите пароль от сервера (который вы получили при создании VM).

### Шаг 3: На сервере - выполните команды

**ТЕПЕРЬ вы на сервере!** Выполните:

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Python
sudo apt install -y python3-pip python3-venv git curl

# Проверка GPU
nvidia-smi

# Клонирование репозитория
cd ~
git clone https://github.com/Human221/resume-ml.git
cd resume-ml

# Создание окружения
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip

# Установка PyTorch
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# Установка зависимостей
pip install -r requirements-train.txt

# Проверка
python3 -c "import torch; print(f'CUDA: {torch.cuda.is_available()}')"
```

---

## 🔍 Как понять, где вы находитесь?

### На Mac:
```bash
# Покажет: Darwin (macOS)
uname -a

# Покажет: MacBook-Pro-Rustam или подобное
hostname
```

### На сервере Cloud.ru:
```bash
# Покажет: Linux
uname -a

# Покажет: что-то вроде ubuntu или ip-xxx
hostname

# Покажет IP: 176.109.111.108
hostname -I
```

---

## 📝 Чеклист

- [ ] На Mac: код готов в репозитории ✅
- [ ] Подключился к серверу: `ssh root@176.109.111.108`
- [ ] На сервере: выполнил `sudo apt update`
- [ ] На сервере: установил Python и зависимости
- [ ] На сервере: проверил GPU (`nvidia-smi`)
- [ ] На сервере: клонировал репозиторий
- [ ] На сервере: установил PyTorch и зависимости
- [ ] На сервере: запустил обучение

---

## 🆘 Если не можете подключиться к серверу

1. Проверьте, что сервер запущен в панели Cloud.ru
2. Проверьте IP адрес: 176.109.111.108
3. Проверьте пароль от сервера
4. Если используете SSH ключи, убедитесь, что они настроены

---

## 📚 Документация

- [SERVER_SETUP.md](SERVER_SETUP.md) - настройка сервера (выполнять НА СЕРВЕРЕ)
- [CLOUDRU_QUICKSTART.md](CLOUDRU_QUICKSTART.md) - быстрый старт (выполнять НА СЕРВЕРЕ)
- [MAC_SETUP.md](MAC_SETUP.md) - что можно делать на Mac

