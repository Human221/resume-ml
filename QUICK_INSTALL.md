# 🚀 Быстрая установка на сервере

## Вариант 1: Автоматическая установка (рекомендуется)

### На вашем Mac:

```bash
cd ~/resume-ml

# Скопируйте скрипт на сервер и запустите его
./remote_execute.sh
```

Введите пароль от сервера, когда попросит.

### Что делает скрипт:

1. Копирует `setup_server.sh` на сервер
2. Запускает автоматическую установку
3. Устанавливает все зависимости
4. Проверяет установку

## Вариант 2: Ручная установка

### Шаг 1: Подключитесь к серверу

```bash
ssh root@176.109.111.108
```

### Шаг 2: Скопируйте и запустите скрипт

На сервере выполните:

```bash
# Клонируйте репозиторий
cd ~
git clone https://github.com/Human221/resume-ml.git
cd resume-ml

# Сделайте скрипт исполняемым и запустите
chmod +x setup_server.sh
./setup_server.sh
```

Скрипт автоматически:
- ✅ Обновит систему
- ✅ Установит Python и зависимости
- ✅ Проверит/установит NVIDIA драйвер
- ✅ Создаст виртуальное окружение
- ✅ Установит PyTorch с CUDA
- ✅ Установит все зависимости
- ✅ Проверит установку

## После установки

### Проверка датасета:

```bash
cd ~/resume-ml
source venv/bin/activate
python download_hf_dataset.py --dataset evilfreelancer/headhunter --action list
```

### Быстрый тест обучения:

```bash
python train_model.py \
    --use-hf \
    --hf-dataset evilfreelancer/headhunter \
    --max-samples 100 \
    --num-epochs 1 \
    --batch-size 2
```

### Полное обучение:

```bash
python train_model.py \
    --use-hf \
    --hf-dataset evilfreelancer/headhunter \
    --output-dir ./models/finetuned \
    --num-epochs 3 \
    --batch-size 4
```

## ⚠️ Если что-то пошло не так

1. Проверьте подключение: `ssh root@176.109.111.108`
2. Проверьте GPU: `nvidia-smi`
3. Если GPU не работает: `sudo ubuntu-drivers autoinstall && sudo reboot`
4. После перезагрузки снова запустите `./setup_server.sh`

