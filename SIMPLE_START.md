# 🚀 Простой старт - пошаговая инструкция

## Вариант 1: Через консоль Cloud.ru (БЕЗ пароля!)

Если у вас есть доступ к консоли VM в панели Cloud.ru:

1. **Откройте панель Cloud.ru**
2. **Найдите вашу VM** (176.109.111.108)
3. **Нажмите "Консоль"** или "Console" (обычно есть кнопка в интерфейсе)
4. **В открывшейся консоли выполните:**

```bash
# 1. Клонируйте репозиторий
cd ~
git clone https://github.com/Human221/resume-ml.git
cd resume-ml

# 2. Запустите автоматическую установку
chmod +x setup_server.sh
./setup_server.sh
```

Скрипт установит все автоматически!

## Вариант 2: Установка пароля и подключение

### Шаг 1: Установите пароль через консоль Cloud.ru

1. Откройте консоль VM в панели Cloud.ru
2. Выполните:
```bash
sudo passwd root
```
3. Введите новый пароль дважды
4. Запомните пароль!

### Шаг 2: Подключитесь с вашего Mac

```bash
ssh root@176.109.111.108
```

Введите пароль, который только что установили.

### Шаг 3: На сервере выполните

```bash
cd ~
git clone https://github.com/Human221/resume-ml.git
cd resume-ml
chmod +x setup_server.sh
./setup_server.sh
```

## Вариант 3: Попробуйте другого пользователя

Иногда используется пользователь `ubuntu` вместо `root`:

```bash
# Попробуйте подключиться как ubuntu
ssh ubuntu@176.109.111.108

# Или попробуйте без указания пользователя
ssh 176.109.111.108
```

## Вариант 4: Через веб-консоль Cloud.ru (самый простой!)

1. **Войдите в панель Cloud.ru**
2. **Найдите вашу VM** (IP: 176.109.111.108)
3. **Нажмите кнопку "Консоль"** или "VNC Console"
4. **В открывшемся окне консоли** скопируйте и выполните:

```bash
# Все команды одной строкой (скопируйте и вставьте):
cd ~ && git clone https://github.com/Human221/resume-ml.git && cd resume-ml && chmod +x setup_server.sh && ./setup_server.sh
```

Это запустит полную автоматическую установку!

## После установки

Когда установка завершится, запустите обучение:

```bash
cd ~/resume-ml
source venv/bin/activate

# Быстрый тест
python train_model.py --use-hf --hf-dataset evilfreelancer/headhunter --max-samples 100 --num-epochs 1 --batch-size 2

# Или полное обучение
python train_model.py --use-hf --hf-dataset evilfreelancer/headhunter --output-dir ./models/finetuned --num-epochs 3 --batch-size 4
```

## ⚡ Самый быстрый способ

**Если есть доступ к консоли Cloud.ru:**

Просто скопируйте и вставьте эту команду в консоль:

```bash
cd ~ && git clone https://github.com/Human221/resume-ml.git && cd resume-ml && chmod +x setup_server.sh && ./setup_server.sh && source venv/bin/activate && python train_model.py --use-hf --hf-dataset evilfreelancer/headhunter --max-samples 100 --num-epochs 1 --batch-size 2
```

Это установит все и запустит тестовое обучение!

