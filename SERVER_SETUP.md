# Настройка сервера Cloud.ru

## 📍 Информация о сервере

- **IP адрес**: 176.109.111.108
- **ОС**: Ubuntu 24.04
- **Конфигурация**: 8 vCPU, 64GB RAM, 256GB диск

## 🔌 Шаг 1: Подключение к серверу

### Подключение по SSH

```bash
ssh root@176.109.111.108
```

Или если используется другой пользователь:

```bash
ssh ваш_пользователь@176.109.111.108
```

**При первом подключении** система попросит подтвердить fingerprint - введите `yes`.

Если запросит пароль, введите пароль, который вы получили при создании VM.

## ✅ Шаг 2: Проверка системы

После подключения проверьте:

```bash
# Проверка версии Ubuntu
lsb_release -a

# Проверка Python
python3 --version

# Проверка GPU (если установлен драйвер)
nvidia-smi

# Проверка дискового пространства
df -h
```

## 📦 Шаг 3: Обновление системы

```bash
sudo apt update
sudo apt upgrade -y
```

## 🐍 Шаг 4: Установка Python и зависимостей

```bash
# Установка необходимых пакетов
sudo apt install -y python3-pip python3-venv git curl

# Проверка версии Python (должна быть 3.10+)
python3 --version
```

## 🎮 Шаг 5: Проверка/установка NVIDIA драйвера и CUDA

### Проверка GPU

```bash
nvidia-smi
```

Если команда не работает, установите драйвер:

```bash
# Обновление списка пакетов
sudo apt update

# Установка NVIDIA драйвера (автоматический выбор)
sudo ubuntu-drivers autoinstall

# Перезагрузка (если потребуется)
sudo reboot
```

После перезагрузки снова подключитесь и проверьте:
```bash
nvidia-smi
```

### Установка CUDA (если нужно)

Обычно CUDA уже включена в PyTorch, но если нужна отдельная установка:

```bash
# Проверка версии CUDA
nvcc --version
```

## 📥 Шаг 6: Клонирование репозитория

```bash
# Переход в домашнюю директорию
cd ~

# Клонирование репозитория
git clone https://github.com/Human221/resume-ml.git

# Переход в директорию проекта
cd resume-ml

# Проверка файлов
ls -la
```

## 🔧 Шаг 7: Создание виртуального окружения

```bash
# Создание виртуального окружения
python3 -m venv venv

# Активация окружения
source venv/bin/activate

# Обновление pip
pip install --upgrade pip
```

## 📚 Шаг 8: Установка PyTorch с CUDA

```bash
# Проверьте версию CUDA на сервере
nvidia-smi

# Установка PyTorch с CUDA 11.8 (наиболее совместимая версия)
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

Если у вас CUDA 12.x, используйте:
```bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```

## 📦 Шаг 9: Установка остальных зависимостей

```bash
# Установка зависимостей для обучения
pip install -r requirements-train.txt
```

Это может занять несколько минут.

## ✅ Шаг 10: Проверка установки

```bash
# Проверка PyTorch и CUDA
python3 -c "import torch; print(f'PyTorch: {torch.__version__}'); print(f'CUDA available: {torch.cuda.is_available()}'); print(f'GPU: {torch.cuda.get_device_name(0) if torch.cuda.is_available() else \"No GPU\"}')"

# Проверка других библиотек
python3 -c "import transformers; import datasets; print('Все библиотеки установлены!')"
```

## 🔍 Шаг 11: Проверка датасета

```bash
# Проверка доступных splits
python download_hf_dataset.py --dataset evilfreelancer/headhunter --action list

# Просмотр образцов данных
python download_hf_dataset.py --dataset evilfreelancer/headhunter --action sample --num-samples 3
```

## 🚀 Шаг 12: Готово к обучению!

Теперь можно запускать обучение. См. следующие шаги в [CLOUDRU_QUICKSTART.md](CLOUDRU_QUICKSTART.md)

## 📝 Быстрая команда для проверки всего

```bash
echo "=== Система ===" && \
lsb_release -a && \
echo -e "\n=== Python ===" && \
python3 --version && \
echo -e "\n=== GPU ===" && \
nvidia-smi && \
echo -e "\n=== PyTorch ===" && \
python3 -c "import torch; print(f'PyTorch: {torch.__version__}'); print(f'CUDA: {torch.cuda.is_available()}')" && \
echo -e "\n=== Диск ===" && \
df -h
```

## ⚠️ Troubleshooting

### Проблема: "Permission denied" при SSH

Проверьте правильность пользователя и пароля. Если используете ключи SSH:
```bash
ssh -i путь/к/ключу root@176.109.111.108
```

### Проблема: nvidia-smi не работает

1. Убедитесь, что GPU добавлен в конфигурацию VM
2. Установите драйвер: `sudo ubuntu-drivers autoinstall`
3. Перезагрузите сервер: `sudo reboot`

### Проблема: Медленная установка пакетов

Используйте зеркало PyPI:
```bash
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

### Проблема: Нехватка места на диске

Проверьте использование:
```bash
df -h
du -sh ~/*
```

Очистите кэш pip:
```bash
pip cache purge
```

## 📞 Следующие шаги

После успешной настройки переходите к:
- [CLOUDRU_QUICKSTART.md](CLOUDRU_QUICKSTART.md) - быстрый старт обучения
- [CLOUDRU_TRAINING.md](CLOUDRU_TRAINING.md) - подробная инструкция

